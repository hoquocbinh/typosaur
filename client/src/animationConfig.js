export const CONFIG = {
    // Animation timing (seconds)
    EXPLOSION_DURATION: 0.5,
    FLIGHT_DURATION: 0.5,
    STAGGER_MAX_DELAY: 0.5,

    // Visual Physics
    MIN_RADIUS: 300,  // Min scatter distance
    MAX_RADIUS: 500, // Max scatter distance
    ROTATION_RANGE: 160, // Character spin amount
};
