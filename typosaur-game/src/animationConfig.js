export const CONFIG = {
    // Animation timing (seconds)
    EXPLOSION_DURATION: 0.5,
    FLIGHT_DURATION: 1.2,
    STAGGER_MAX_DELAY: 0.5,

    // Visual Physics
    MIN_RADIUS: 300,  // Min scatter distance
    MAX_RADIUS: 600, // Max scatter distance (increased for "further")
    ROTATION_RANGE: 360, // Character spin amount (increased spin)
};
